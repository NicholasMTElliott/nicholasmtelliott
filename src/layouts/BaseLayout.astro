---
interface Props {
  title?: string
}

const { title = 'Nicholas M. T. Elliott' } = Astro.props
const currentPath = Astro.url.pathname

const nav = [
  { href: '/', label: 'Home' },
  { href: '/case-studies/global-ag-platform', label: 'Case Study' },
  { href: '/ai', label: 'AI' },
  { href: '/portfolio', label: 'Portfolio' },
  { href: '/about', label: 'About' },
]

const isActive = (href: string) => {
  if (href === '/') {
    return currentPath === '/'
  }

  return currentPath.startsWith(href)
}
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <link rel="icon" href="/favicon.ico" />
    <link rel="preconnect" href="https://gravatar.com" />
  </head>
  <body>
    <header class="site-header">
      <div class="site-header__inner">
        <a class="identity" href="/">
          <img
            class="identity__avatar"
            src="https://gravatar.com/avatar/468788fd550c4128b0c250566ea8729f?s=128"
            alt="Nicholas Elliott avatar"
          />
          <div>
            <h1>Nicholas M.T. Elliott</h1>
            <p>Director of Engineering · Platform Architecture · AI in Production</p>
          </div>
        </a>
        <nav class="site-nav" aria-label="Primary navigation">
          {
            nav.map(link => (
              <a class={isActive(link.href) ? 'active' : ''} href={link.href}>
                {link.label}
              </a>
            ))
          }
        </nav>
      </div>
    </header>
    <main class="site-main">
      <slot />
    </main>
  </body>
</html>

<style is:global lang="scss">
  @use '../styles/global.scss';
</style>
